from utils import evaluate_bitwise_or

    if __name__ == "__main__":
        # テストケース
        target = "10000000"
        mask = "11000000"

    # 使用例
    evaluate_bitwise_or("10000000", "11000000")

    # 結果を確認
    result = evaluate_bitwise_or(target, mask)
    print(result)  # 期待値: True
